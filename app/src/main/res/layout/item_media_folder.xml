<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/row_folder_height"
    android:background="@drawable/bg_row_selector"
    android:clickable="true"
    android:focusable="true"
    android:focusableInTouchMode="false">

    <!-- Folder Row Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <!-- Folder Icon with Corner Red Badge Container -->
        <FrameLayout
            android:layout_width="48dp"
            android:layout_height="44dp"
            android:layout_gravity="center_vertical">

            <!-- Folder Icon Silhouette -->
            <ImageView
                android:id="@+id/folderIcon"
                android:layout_width="44dp"
                android:layout_height="40dp"
                android:layout_gravity="bottom|start"
                android:contentDescription="Folder"
                android:src="@drawable/ic_mx_folder_default" />

            <!-- Red Circular New Items Badge (Top-Right of Folder Icon) -->
            <TextView
                android:id="@+id/newBadgeTxt"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="top|end"
                android:background="@drawable/badge_circle_red"
                android:gravity="center"
                android:includeFontPadding="false"
                android:text="1"
                android:textColor="@android:color/white"
                android:textSize="10sp"
                android:textStyle="bold"
                android:visibility="gone"
                tools:visibility="visible" />
        </FrameLayout>

        <!-- Folder Name (Line 1) & Video Count Subtitle (Line 2) -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:orientation="vertical">

            <TextView
                android:id="@+id/folderNameTxt"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="?android:attr/textColorPrimary"
                android:textSize="16sp"
                android:textStyle="bold"
                tools:text="Camera" />

            <TextView
                android:id="@+id/videoCountTxt"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="?android:attr/textColorSecondary"
                android:textSize="13sp"
                tools:text="27 videos" />
        </LinearLayout>
    </LinearLayout>

    <!-- Hairline Divider with standard MX 72dp indentation -->
    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/divider_hairline"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="72dp"
        android:background="@color/mx_divider_subtle" />

</RelativeLayout>
